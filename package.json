{
  "name": "proto",
  "version": "0.1.0",
  "description": "Continuous subcontractor license verification for construction projects.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "serve": "node --env-file-if-exists=.env src/server.js",
    "sweep": "node --env-file-if-exists=.env src/sweep.js",
    "brief": "node --env-file-if-exists=.env src/run.js",
    "test": "node --test"
  },
  "dependencies": {
    "@google/genai": "^2.22.0",
    "@strands-agents/sdk": "^1.17.0",
    "openai": "^6.49.0",
    "zod": "^4.0.0"
  }
}
